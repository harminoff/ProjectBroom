diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index cc079f80af24b86ba6b0114e31828054c7d78789..88456a37a87ea3d16380adc44cf0f7d701ffba88 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -1221,7 +1221,13 @@ else()
 	set( NOT_COMPILED_SOURCE_FILES ${NOT_COMPILED_SOURCE_FILES} ${VM_JIT_SOURCES} )
 endif()
 
+set(PROJECT_BROOM_ROOT "" CACHE PATH "Project Broom source root")
+if(NOT EXISTS "${PROJECT_BROOM_ROOT}/src/gzdoom-bridge/brogue_bridge_frontend.cpp")
+	message(FATAL_ERROR "Set PROJECT_BROOM_ROOT to the Project Broom source root")
+endif()
+
 set( GAME_SOURCES
+	${PROJECT_BROOM_ROOT}/src/gzdoom-bridge/brogue_bridge_frontend.cpp
 	${HEADER_FILES}
 	${NOT_COMPILED_SOURCE_FILES}
 	${SYSTEM_SOURCES}
@@ -1332,6 +1338,8 @@ target_link_libraries( zdoom ${PROJECT_LIBRARIES} lzma ${ZMUSIC_LIBRARIES} absl:
 include_directories(
 	BEFORE
 	.
+	${PROJECT_BROOM_ROOT}/src/gzdoom-bridge
+	${PROJECT_BROOM_ROOT}/src/brogue-mapgen/src/brogue
 	common/audio/sound
 	common/audio/music
 	common/2d
diff --git a/src/d_main.cpp b/src/d_main.cpp
index 35d4bf4ab4f8d34eaac3357c86c6c9b6da656a62..1db63f5b942d925ebb4059e1df4d654ad8f0847d 100644
--- a/src/d_main.cpp
+++ b/src/d_main.cpp
@@ -24,6 +24,7 @@
 // HEADER FILES ------------------------------------------------------------
 
 #include "c_cvars.h"
+#include "brogue_bridge_frontend.h"
 #include "i_net.h"
 #include "i_soundinternal.h"
 
@@ -1115,6 +1116,7 @@ void D_Display ()
 	GC::CheckGC();
 
 	SetDeltaTime();
+	if (!AppActive) BrogueBridge_CancelSearchForUi();
 
 	if (nodrawers || screen == NULL)
 		return; 				// for comparative timing / profiling
@@ -3989,7 +3991,7 @@ static int D_DoomMain_Internal (void)
 		StrTable_ValidFilter,
 		nullptr,
 		CheckSkipGameOptionBlock,
-		System_ConsoleToggled,
+		[](int state) { BrogueBridge_CancelSearchForUi(); System_ConsoleToggled(state); },
 		nullptr,
 		nullptr,
 		System_ToggleFullConsole,
@@ -3998,11 +4000,11 @@ static int D_DoomMain_Internal (void)
 		CheckCheatmode,
 		System_HudScaleChanged,
 		M_SetSpecialMenu,
-		OnMenuOpen,
+		[](bool sound) { BrogueBridge_CancelSearchForUi(); OnMenuOpen(sound); },
 		System_LanguageChanged,
 		OkForLocalization,
 		[]() ->FConfigFile* { return GameConfig; },
-		nullptr,
+		BrogueBridge_WantsSearchEscape,
 		RemapUserTranslation,
 		System_DisableAnisotropicFiltering
 	};
diff --git a/src/g_game.cpp b/src/g_game.cpp
index 3f7d18b4958dd6624f3127483df9e7d946e39883..d02b127a7e09beb4275e856ff40b9d2dc3e35608 100644
--- a/src/g_game.cpp
+++ b/src/g_game.cpp
@@ -19,6 +19,7 @@
 
 // HEADER FILES ------------------------------------------------------------
 
+#include "brogue_bridge_frontend.h"
 #include <memory>
 #include <stdio.h>
 #include <string.h>
@@ -899,6 +900,7 @@ void G_BuildTiccmd (usercmd_t *cmd)
 	cmd->forwardmove <<= 8;
 	cmd->sidemove <<= 8;
 
+	BrogueBridge_PrepareTiccmd(cmd);
 	buttons = cmd->buttons;
 }
 
@@ -1061,6 +1063,9 @@ CCMD (spycancel)
 //
 bool G_Responder (event_t *ev)
 {
+	if (BrogueBridge_HandleInput(ev))
+		return true;
+
 	if (!CanChat())
 		CT_Stop();
 
diff --git a/src/g_statusbar/shared_sbar.cpp b/src/g_statusbar/shared_sbar.cpp
index 26a1c7f7a9feaaeda6d330857ab34a5a54cde801..33d17484f5d8aae42252f890f1940c9cf9e3f740 100644
--- a/src/g_statusbar/shared_sbar.cpp
+++ b/src/g_statusbar/shared_sbar.cpp
@@ -22,6 +22,7 @@
 **
 */
 
+#include "brogue_bridge_frontend.h"
 #include <assert.h>
 
 #include "c_cvars.h"
@@ -1180,6 +1181,7 @@ void DBaseStatusBar::DrawBottomStuff (EHudState state)
 
 void DBaseStatusBar::DrawTopStuff (EHudState state)
 {
+	BrogueBridge_DrawHud();
 	if (demoplayback && demover != DEMOGAMEVERSION)
 	{
 		DrawText(twod, SmallFont, CR_TAN, 0, GetTopOfStatusbar() - 40 * CleanYfac,
diff --git a/src/menu/doommenu.cpp b/src/menu/doommenu.cpp
index e0925eb2634afd895e8eb27d14193f71393fa71d..c47b8f231dcb06a4727fa5950594697a22b379ba 100644
--- a/src/menu/doommenu.cpp
+++ b/src/menu/doommenu.cpp
@@ -21,6 +21,7 @@
 **
 */
 
+#include "brogue_bridge_frontend.h"
 #include "c_bind.h"
 #include "c_dispatch.h"
 #include "d_event.h"
@@ -230,6 +231,8 @@ bool M_SetSpecialMenu(FName& menu, int param)
 		NewGameStartupInfo.Skill = param;
 		[[fallthrough]];
 	case NAME_StartgameConfirmed:
+		// Discard the old Brogue session before the prepared campaign restarts.
+		BrogueBridge_Shutdown();
 
 		G_DeferedInitNew (&NewGameStartupInfo);
 		if (gamestate == GS_FULLCONSOLE)
diff --git a/src/playsim/p_user.cpp b/src/playsim/p_user.cpp
index 23ba8dbddfcd978d557a2e9c5df013882344b496..86d21ef2f9bf779d45042296e47d76357b91528d 100644
--- a/src/playsim/p_user.cpp
+++ b/src/playsim/p_user.cpp
@@ -29,6 +29,7 @@
 */
 
 #include "a_keys.h"
+#include "brogue_bridge_frontend.h"
 #include "a_morph.h"
 #include "actorinlines.h" // IWYU pragma: keep
 #include "basics.h"
@@ -1780,6 +1781,10 @@ static void P_RollbackPlayers(FSerializer& arc)
 
 void P_PredictClient()
 {
+	// Project Broom projects committed Brogue positions, never Doom predictions.
+	if (BrogueBridge_OwnsPlayerPosition())
+		return;
+
 	if (gamestate != GS_LEVEL)
 		return;
 
