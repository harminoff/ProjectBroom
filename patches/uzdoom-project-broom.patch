diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index cc079f80af24b86ba6b0114e31828054c7d78789..88456a37a87ea3d16380adc44cf0f7d701ffba88 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -1221,7 +1221,13 @@ else()
 	set( NOT_COMPILED_SOURCE_FILES ${NOT_COMPILED_SOURCE_FILES} ${VM_JIT_SOURCES} )
 endif()
 
+set(PROJECT_BROOM_ROOT "" CACHE PATH "Project Broom source root")
+if(NOT EXISTS "${PROJECT_BROOM_ROOT}/src/gzdoom-bridge/brogue_bridge_frontend.cpp")
+	message(FATAL_ERROR "Set PROJECT_BROOM_ROOT to the Project Broom source root")
+endif()
+
 set( GAME_SOURCES
+	${PROJECT_BROOM_ROOT}/src/gzdoom-bridge/brogue_bridge_frontend.cpp
 	${HEADER_FILES}
 	${NOT_COMPILED_SOURCE_FILES}
 	${SYSTEM_SOURCES}
@@ -1332,6 +1338,8 @@ target_link_libraries( zdoom ${PROJECT_LIBRARIES} lzma ${ZMUSIC_LIBRARIES} absl:
 include_directories(
 	BEFORE
 	.
+	${PROJECT_BROOM_ROOT}/src/gzdoom-bridge
+	${PROJECT_BROOM_ROOT}/src/brogue-mapgen/src/brogue
 	common/audio/sound
 	common/audio/music
 	common/2d
diff --git a/src/common/console/c_enginecmds.cpp b/src/common/console/c_enginecmds.cpp
index aff36fa707a4ed8d2a93e6818b1edfd9ff8a4d4a..94deb222063178e7dfa60faa7de3c04867cc513f 100644
--- a/src/common/console/c_enginecmds.cpp
+++ b/src/common/console/c_enginecmds.cpp
@@ -1,3 +1,4 @@
+#include "brogue_bridge_frontend.h"
 /*
 ** c_enginecmds.cpp
 **
@@ -51,12 +52,14 @@ extern FILE* Logfile;
 void ConsoleEndoom();
 CCMD (quit)
 {
+	if (!BrogueBridge_SaveOnClose()) return;
 	ConsoleEndoom();
 	throw CExitEvent(0);
 }
 
 CCMD (exit)
 {
+	if (!BrogueBridge_SaveOnClose()) return;
 	ConsoleEndoom();
 	throw CExitEvent(0);
 }
diff --git a/src/common/platform/win32/i_input.cpp b/src/common/platform/win32/i_input.cpp
index 7e8c49c8198fa4c5fd797768229238e0376e1209..3cfa9d91ff9d792f12f039be585bb73fa3d75c70 100644
--- a/src/common/platform/win32/i_input.cpp
+++ b/src/common/platform/win32/i_input.cpp
@@ -1,3 +1,4 @@
+#include "brogue_bridge_frontend.h"
 /*
 ** i_input.cpp
 **
@@ -382,6 +383,10 @@ LRESULT CALLBACK WndProc (HWND hWnd, UINT message, WPARAM wParam, LPARAM lParam)
 
 	switch (message)
 	{
+	case WM_CLOSE:
+        if (BrogueBridge_SaveOnClose()) PostQuitMessage(0);
+        return 0;
+
 	case WM_DESTROY:
 		SetPriorityClass (GetCurrentProcess(), NORMAL_PRIORITY_CLASS);
 		PostQuitMessage (0);
@@ -567,8 +572,10 @@ void I_GetWindowEvent()
 
 	while (PeekMessage (&mess, NULL, 0, 0, PM_REMOVE))
 	{
-		if (mess.message == WM_QUIT)
-			throw CExitEvent(mess.wParam);
+		if (mess.message == WM_QUIT) {
+            if (BrogueBridge_SaveOnClose()) throw CExitEvent(mess.wParam);
+            continue;
+        }
 
 		if (GUICapture)
 		{
diff --git a/src/d_main.cpp b/src/d_main.cpp
index 35d4bf4ab4f8d34eaac3357c86c6c9b6da656a62..0863a71e2d7d4c1992ebfc0fed5c8b19b894fc7a 100644
--- a/src/d_main.cpp
+++ b/src/d_main.cpp
@@ -24,6 +24,7 @@
 // HEADER FILES ------------------------------------------------------------
 
 #include "c_cvars.h"
+#include "brogue_bridge_frontend.h"
 #include "i_net.h"
 #include "i_soundinternal.h"
 
@@ -1084,6 +1085,7 @@ static void DrawRateStuff()
 static void DrawOverlays()
 {
 	C_DrawConsole ();
+    BrogueBridge_DrawPersistence();
 	M_Drawer ();
 	DrawRateStuff();
 	if (!hud_toggled)
@@ -1115,6 +1117,8 @@ void D_Display ()
 	GC::CheckGC();
 
 	SetDeltaTime();
+	BrogueBridge_TickPersistence();
+	if (!AppActive) BrogueBridge_CancelSearchForUi();
 
 	if (nodrawers || screen == NULL)
 		return; 				// for comparative timing / profiling
@@ -1216,6 +1220,7 @@ void D_Display ()
 	screen->FrameTime = I_msTimeFS();
 	TexAnim.UpdateAnimations(screen->FrameTime);
 	R_UpdateSky(I_GetTimeFrac());
+	if (gamestate == GS_LEVEL) BrogueBridge_PrepareScene();
 	screen->BeginFrame();
 	twod->ClearClipRect();
 	if ((gamestate == GS_LEVEL || gamestate == GS_TITLELEVEL) && gametic != 0)
@@ -3989,7 +3994,7 @@ static int D_DoomMain_Internal (void)
 		StrTable_ValidFilter,
 		nullptr,
 		CheckSkipGameOptionBlock,
-		System_ConsoleToggled,
+		[](int state) { BrogueBridge_CancelSearchForUi(); System_ConsoleToggled(state); },
 		nullptr,
 		nullptr,
 		System_ToggleFullConsole,
@@ -3998,11 +4003,11 @@ static int D_DoomMain_Internal (void)
 		CheckCheatmode,
 		System_HudScaleChanged,
 		M_SetSpecialMenu,
-		OnMenuOpen,
+		[](bool sound) { BrogueBridge_CancelSearchForUi(); OnMenuOpen(sound); },
 		System_LanguageChanged,
 		OkForLocalization,
 		[]() ->FConfigFile* { return GameConfig; },
-		nullptr,
+		BrogueBridge_WantsSearchEscape,
 		RemapUserTranslation,
 		System_DisableAnisotropicFiltering
 	};
diff --git a/src/g_game.cpp b/src/g_game.cpp
index 3f7d18b4958dd6624f3127483df9e7d946e39883..f44cfdaa853db52b13b7b454e26e1ed9880f7e55 100644
--- a/src/g_game.cpp
+++ b/src/g_game.cpp
@@ -19,6 +19,7 @@
 
 // HEADER FILES ------------------------------------------------------------
 
+#include "brogue_bridge_frontend.h"
 #include <memory>
 #include <stdio.h>
 #include <string.h>
@@ -899,6 +900,7 @@ void G_BuildTiccmd (usercmd_t *cmd)
 	cmd->forwardmove <<= 8;
 	cmd->sidemove <<= 8;
 
+	BrogueBridge_PrepareTiccmd(cmd);
 	buttons = cmd->buttons;
 }
 
@@ -1061,6 +1063,9 @@ CCMD (spycancel)
 //
 bool G_Responder (event_t *ev)
 {
+	if (BrogueBridge_HandleInput(ev))
+		return true;
+
 	if (!CanChat())
 		CT_Stop();
 
@@ -1931,6 +1936,7 @@ void G_ScreenShot (const char *filename)
 //
 void G_LoadGame (const char* name, bool hidecon)
 {
+    if (BrogueBridge_UsesNativeSaves()) { Printf("Use Project Broom Load or Import for native Brogue saves.\n"); return; }
 	if (name != NULL)
 	{
 		savename = name;
@@ -2042,6 +2048,7 @@ void C_SerializeCVars(FSerializer& arc, const char* label, uint32_t filter)
 
 void G_DoLoadGame ()
 {
+    if (BrogueBridge_UsesNativeSaves()) { gameaction = ga_nothing; return; }
 	Net_ResetCommands(true);
 	SetupLoadingCVars();
 	bool hidecon;
@@ -2238,6 +2245,7 @@ void G_DoLoadGame ()
 //
 void G_SaveGame (const char *filename, const char *description, bool quick)
 {
+    if (BrogueBridge_UsesNativeSaves()) { AddCommandString("brg_save_exit"); return; }
 	if (sendsave || gameaction == ga_savegame || gameaction == ga_quicksave)
 	{
 		Printf ("%s\n", GStrings.GetString("TXT_SAVEPENDING"));
@@ -2405,6 +2413,7 @@ static void PutSavePic (FileWriter *file, int width, int height)
 
 void G_DoSaveGame (bool okForQuicksave, bool forceQuicksave, FString filename, const char *description)
 {
+    if (BrogueBridge_UsesNativeSaves()) return;
 	TArray<FCompressedBuffer> savegame_content;
 	TArray<FString> savegame_filenames;
 
diff --git a/src/g_statusbar/shared_sbar.cpp b/src/g_statusbar/shared_sbar.cpp
index 26a1c7f7a9feaaeda6d330857ab34a5a54cde801..33d17484f5d8aae42252f890f1940c9cf9e3f740 100644
--- a/src/g_statusbar/shared_sbar.cpp
+++ b/src/g_statusbar/shared_sbar.cpp
@@ -22,6 +22,7 @@
 **
 */
 
+#include "brogue_bridge_frontend.h"
 #include <assert.h>
 
 #include "c_cvars.h"
@@ -1180,6 +1181,7 @@ void DBaseStatusBar::DrawBottomStuff (EHudState state)
 
 void DBaseStatusBar::DrawTopStuff (EHudState state)
 {
+	BrogueBridge_DrawHud();
 	if (demoplayback && demover != DEMOGAMEVERSION)
 	{
 		DrawText(twod, SmallFont, CR_TAN, 0, GetTopOfStatusbar() - 40 * CleanYfac,
diff --git a/src/menu/doommenu.cpp b/src/menu/doommenu.cpp
index e0925eb2634afd895e8eb27d14193f71393fa71d..b827976aa1b6d99dce28ac78a59ad711472ff611 100644
--- a/src/menu/doommenu.cpp
+++ b/src/menu/doommenu.cpp
@@ -21,6 +21,7 @@
 **
 */
 
+#include "brogue_bridge_frontend.h"
 #include "c_bind.h"
 #include "c_dispatch.h"
 #include "d_event.h"
@@ -230,6 +231,9 @@ bool M_SetSpecialMenu(FName& menu, int param)
 		NewGameStartupInfo.Skill = param;
 		[[fallthrough]];
 	case NAME_StartgameConfirmed:
+		if (!BrogueBridge_ConfirmNewGame()) return false;
+		// Discard the old Brogue session before the prepared campaign restarts.
+		BrogueBridge_Shutdown();
 
 		G_DeferedInitNew (&NewGameStartupInfo);
 		if (gamestate == GS_FULLCONSOLE)
@@ -490,6 +494,7 @@ CCMD (menu_endgame)
 
 CCMD (quicksave)
 {	// F6
+    if (BrogueBridge_UsesNativeSaves()) { AddCommandString("brg_save_exit"); return; }
 	if (!usergame || (players[consoleplayer].health <= 0 && !multiplayer))
 	{
 		S_Sound (CHAN_VOICE, CHANF_UI|(haptics_do_menus?CHANF_RUMBLE:CHANF_NORUMBLE), "menu/invalid", snd_menuvolume, ATTN_NONE);
@@ -545,6 +550,7 @@ CCMD (quicksave)
 
 CCMD (quickload)
 {	// F9
+    if (BrogueBridge_UsesNativeSaves()) { M_StartControlPanel(true); M_SetMenu("BrogueSaves", -1); return; }
 	if (netgame)
 	{
 		M_StartControlPanel(true);
@@ -1474,12 +1480,14 @@ CCMD (menu_main)
 
 CCMD (menu_load)
 {	// F3
+    if (BrogueBridge_UsesNativeSaves()) { M_StartControlPanel(true); M_SetMenu("BrogueSaves", -1); return; }
 	M_StartControlPanel (true);
 	M_SetMenu(NAME_LoadgameMenu, -1);
 }
 
 CCMD (menu_save)
 {	// F2
+    if (BrogueBridge_UsesNativeSaves()) { AddCommandString("brg_save_exit"); return; }
 	M_StartControlPanel (true);
 	M_SetMenu(NAME_SavegameMenu, -1);
 }
diff --git a/src/p_openmap.cpp b/src/p_openmap.cpp
index df979f2a861a0059e7856c7a6c0f5a6079264a53..d50c13d30cf1a5c2c778ada148998cfcfc68849b 100644
--- a/src/p_openmap.cpp
+++ b/src/p_openmap.cpp
@@ -1,3 +1,4 @@
+#include "brogue_bridge_frontend.h"
 /*
 ** p_openmap.cpp
 **
@@ -101,6 +102,8 @@ static int GetMapIndex(const char *mapname, int lastindex, const char *lumpname,
 
 MapData *P_OpenMapData(const char * mapname, bool justcheck)
 {
+    const char *overridePath = BrogueBridge_MapOverride(mapname);
+    if (overridePath) mapname = overridePath;
 	MapData * map = new MapData;
 	FileReader * wadReader = nullptr;
 	bool externalfile = !strnicmp(mapname, "file:", 5);
diff --git a/src/playsim/p_user.cpp b/src/playsim/p_user.cpp
index 23ba8dbddfcd978d557a2e9c5df013882344b496..86d21ef2f9bf779d45042296e47d76357b91528d 100644
--- a/src/playsim/p_user.cpp
+++ b/src/playsim/p_user.cpp
@@ -29,6 +29,7 @@
 */
 
 #include "a_keys.h"
+#include "brogue_bridge_frontend.h"
 #include "a_morph.h"
 #include "actorinlines.h" // IWYU pragma: keep
 #include "basics.h"
@@ -1780,6 +1781,10 @@ static void P_RollbackPlayers(FSerializer& arc)
 
 void P_PredictClient()
 {
+	// Project Broom projects committed Brogue positions, never Doom predictions.
+	if (BrogueBridge_OwnsPlayerPosition())
+		return;
+
 	if (gamestate != GS_LEVEL)
 		return;
 
